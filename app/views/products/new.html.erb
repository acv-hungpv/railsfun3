<%= semantic_form_for @product do |f| %>
	<%= f.input :title %>
	<%= f.input :description %>
	<%= f.input :price, min: 0 %>
	<%= f.input :category %>
	<%= f.country_select :country %> 
	<%= f.action :submit %>
<% end %>
